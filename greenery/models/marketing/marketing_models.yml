version: 2

models:
  - name: fact_user_orders
    description: "Fact table orders by user aggregated"
    columns:
      - name: user_id
        description: "Primary Key"
        tests:
          - unique
          - not_null